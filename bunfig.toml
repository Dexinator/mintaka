[install]
# Set default registry
registry = "https://registry.npmjs.org"

# GSAP is now free since April 2025, no private registry needed
# [install.scopes]
# "@gsap" = { url = "https://npm.greensock.com", token = "$GSAP_TOKEN" }
